---
name: plan-menu
description: Alias for /plan-meal. Part of the yes-chef suite.
---

# /plan-menu

Alias for `/plan-meal`. Same behavior, same flow, same outputs.

**REQUIRED SKILL:** Invoke `/plan-meal` via the Skill tool — its `SKILL.md` contains the complete protocol (interview flow, file formats, UX rules, self-review, section breakdown, model selection).

This file exists purely to offer a second intuitive invocation name — "plan-menu" matches the MENU file suffix, while "plan-meal" matches the full chef-suite framing. Both commands do exactly the same thing.

If `/plan-meal` is unavailable for some reason, read `../plan-meal/SKILL.md` directly and follow it verbatim.
